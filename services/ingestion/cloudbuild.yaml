# Build context is the repo root; the Dockerfile stays next to its service.
steps:
  - name: gcr.io/cloud-builders/docker
    args:
      - build
      - --file=services/ingestion/Dockerfile
      - --tag=${_IMAGE}
      - .
images:
  - ${_IMAGE}
options:
  logging: CLOUD_LOGGING_ONLY
