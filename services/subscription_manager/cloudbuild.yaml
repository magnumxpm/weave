steps:
  - name: gcr.io/cloud-builders/docker
    args:
      - build
      - --file=services/subscription_manager/Dockerfile
      - --tag=${_IMAGE}
      - .
images:
  - ${_IMAGE}
options:
  logging: CLOUD_LOGGING_ONLY
