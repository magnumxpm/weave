[project]
name = "weave-common"
version = "0.1.0"
description = "Shared Weave data contracts"
requires-python = ">=3.12,<3.13"
dependencies = ["pydantic>=2.11.0"]

[tool.uv.build-backend]
module-name = "weave_common"
module-root = ""

[build-system]
requires = ["uv_build>=0.12.3,<0.13.0"]
build-backend = "uv_build"
